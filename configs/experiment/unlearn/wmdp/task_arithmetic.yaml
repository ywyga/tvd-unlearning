# @package _global_
#
# TaskArithmetic unlearn config for WMDP-cyber with phi-1_5.
# Closed-form: no training loop — arithmetic is applied once then model is saved.
#
# Prerequisites:
#   saves/finetune/wmdp_phi-1_5_cyber_full/    — M1 (starting model)
#   saves/finetune/wmdp_phi-1_5_cyber_forget/  — M_forget (forget-only finetune)
#   microsoft/phi-1_5                           — M0 (base model)
#
# Usage (via wmdp_unlearn_phi-1_5.sh — do not run directly):
#   python src/train.py --config-name=unlearn.yaml \
#       experiment=unlearn/wmdp/task_arithmetic.yaml \
#       trainer=TaskArithmetic \
#       task_name=<name> \
#       model.model_args.pretrained_model_name_or_path=saves/finetune/wmdp_phi-1_5_cyber_full \
#       trainer.method_args.base_model_name_or_path=microsoft/phi-1_5 \
#       trainer.method_args.forget_model_path=saves/finetune/wmdp_phi-1_5_cyber_forget \
#       trainer.method_args.scale=1.0

defaults:
  - override /model: phi-1_5
  - override /trainer: TaskArithmetic
  - override /data: unlearn
  - override /data/datasets@data.forget: WMDP_forget
  - override /data/datasets@data.retain: WMDP_retain
  - override /eval: lm_eval

data_split: cyber

data:
  anchor: forget
  forget:
    WMDP_forget:
      args:
        hf_args:
          data_files: data/wmdp/wmdp-corpora/${data_split}-forget-corpus.jsonl
  retain:
    WMDP_retain:
      args:
        hf_args:
          data_files: data/wmdp/wmdp-corpora/${data_split}-retain-corpus.jsonl

eval:
  lm_eval:
    tasks:
      - wmdp_${data_split}
      - mmlu

collator:
  DataCollatorForSupervisedDataset:
    args:
      padding_side: right

task_name: ???
