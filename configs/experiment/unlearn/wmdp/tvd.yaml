# @package _global_
#
# TVD unlearn config for WMDP-cyber with phi-1_5.
#
# Prerequisites:
#   saves/finetune/wmdp_phi-1_5_cyber_full/   — M1 (combined corpus finetune)
#   microsoft/phi-1_5                          — M0 (base model)
#
# Usage (via wmdp_unlearn_phi-1_5.sh — do not run directly):
#   python src/train.py --config-name=unlearn.yaml \
#       experiment=unlearn/wmdp/tvd.yaml \
#       trainer=TVD \
#       task_name=<name> \
#       model.model_args.pretrained_model_name_or_path=saves/finetune/wmdp_phi-1_5_cyber_full \
#       trainer.method_args.base_model_name_or_path=microsoft/phi-1_5 \
#       data.forget.WMDP_forget.args.max_samples=200 \
#       data.forget.WMDP_forget.args.shuffle_seed=42 \
#       data.retain.WMDP_retain.args.max_samples=200 \
#       data.retain.WMDP_retain.args.shuffle_seed=42

defaults:
  - override /model: phi-1_5
  - override /trainer: TVD
  - override /data: unlearn
  - override /data/datasets@data.forget: WMDP_forget
  - override /data/datasets@data.retain: WMDP_retain
  - override /eval: lm_eval

data_split: cyber

data:
  anchor: forget
  forget:
    WMDP_forget:
      args:
        hf_args:
          data_files: data/wmdp/wmdp-corpora/${data_split}-forget-corpus.jsonl
        max_samples: 200
        shuffle_seed: 42
  retain:
    WMDP_retain:
      args:
        hf_args:
          data_files: data/wmdp/wmdp-corpora/${data_split}-retain-corpus.jsonl
        max_samples: 200
        shuffle_seed: 42

eval:
  lm_eval:
    tasks:
      - wmdp_${data_split}
      - mmlu

collator:
  DataCollatorForSupervisedDataset:
    args:
      padding_side: right

trainer:
  args:
    per_device_train_batch_size: 4
    gradient_accumulation_steps: 8
    learning_rate: 1e-5
    num_train_epochs: 5
    gradient_checkpointing: false   # required by TVD (two forward passes per step)
    lr_scheduler_type: constant

task_name: ???
